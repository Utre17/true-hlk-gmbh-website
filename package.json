{
  "name": "true-hlk-static-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "serve": "vite --host 127.0.0.1 --port 4173",
    "build": "vite build",
    "test:responsive": "playwright test tests/responsive.spec.js --reporter=line"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "vite": "^8.0.14"
  },
  "dependencies": {
    "motion": "^12.40.0"
  }
}
